{
  "name": "workforce",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10",
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
